# Pilot's Logbook - Environment Configuration
# Copy this file to .env and customize for your environment

# ============================================
# SECURITY SETTINGS (REQUIRED - CHANGE THESE!)
# ============================================

# Session secret - Use a long random string (minimum 32 characters)
# Generate with: openssl rand -hex 32
SESSION_SECRET=change-this-to-a-random-64-character-string

# Admin login credentials
# Change these immediately after first deployment!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# ============================================
# PORT CONFIGURATION
# ============================================

# Internal application port (container port)
PORT=3000

# External host port (port you'll access from browser)
# Change this if port 3000 is already in use on your system
HOST_PORT=3000

# ============================================
# DATABASE CONFIGURATION
# ============================================

# Path to SQLite database file (inside container)
# Default is fine unless you have a specific reason to change it
DB_PATH=/app/data/logbook.db

# Path to data directory on host machine
# Default is ./data (relative to docker-compose.yml)
DATA_PATH=./data

# ============================================
# CONTAINER CONFIGURATION
# ============================================

# Docker container name
# Change if you're running multiple instances
CONTAINER_NAME=pilots-logbook

# ============================================
# NOTES
# ============================================
# - This file (.env) should NEVER be committed to git
# - All values have sensible defaults except SESSION_SECRET, ADMIN_USERNAME, and ADMIN_PASSWORD
# - If you don't set a value, the default (shown above) will be used
# - After deployment, login and change your password immediately!
