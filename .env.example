# Pilot's Logbook - Environment Configuration
# Copy this file to .env and customize for your environment

# ==================== DEPLOYMENT CONFIGURATION ====================

# Container name (default: pilots-logbook)
CONTAINER_NAME=pilots-logbook

# Host port mapping (default: 3000)
HOST_PORT=3000

# Internal container port (default: 3000)
PORT=3000

# Timezone (default: America/Vancouver)
TZ=America/Vancouver

# Data directory path (default: ./data)
DATA_PATH=./data

# Database file path inside container (default: /app/data/logbook.db)
DB_PATH=/app/data/logbook.db


# ==================== SECURITY SETTINGS ====================

# Session secret - REQUIRED in production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Or use: openssl rand -hex 32
SESSION_SECRET=change-this-to-a-random-string-in-production

# Initial admin credentials
# CHANGE THESE IMMEDIATELY after first deployment!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme


# ==================== REVERSE PROXY CONFIGURATION ====================

# TRUST_PROXY: Set to 'true' ONLY if behind a reverse proxy (nginx, Cloudflare Tunnel, Traefik)
#
# When to use TRUST_PROXY=true:
#   - Behind nginx with SSL termination
#   - Using Cloudflare Tunnel
#   - Using Traefik with HTTPS
#   - Any reverse proxy handling HTTPS
#
# When to use TRUST_PROXY=false (DEFAULT):
#   - Direct HTTP access (http://localhost:3000)
#   - Docker port mapping without reverse proxy
#   - Development environments
#   - ARM devices (Raspberry Pi, Mac) without reverse proxy
#
# Why this matters:
#   - TRUST_PROXY=true enables secure cookies (HTTPS-only)
#   - Secure cookies don't work over plain HTTP
#   - Direct HTTP access with TRUST_PROXY=true = 401 errors
#
# Default: false (works for most deployments)
TRUST_PROXY=false


# ==================== NOTES ====================
# - This file (.env) should NEVER be committed to git
# - All values have sensible defaults except SESSION_SECRET, ADMIN_USERNAME, and ADMIN_PASSWORD
# - If you don't set a value, the default (shown above) will be used
# - After deployment, login and change your password immediately!
